PORT=8787

# mock = proof 0x01 + 9 public inputs (only valid if your on-chain verifier matches this)
# snarkjs = real Groth16 (set ZK_WASM_PATH + ZK_ZKEY_PATH)
ZK_PROVER_IMPLEMENTATION=mock

# Optional: Authorization: Bearer <token>
ZK_PROVER_AUTH_TOKEN=

# Example Railway/container paths (if artifacts are copied into /app/circuits):
ZK_WASM_PATH=/app/circuits/exam_pass.wasm
ZK_ZKEY_PATH=/app/circuits/exam_pass_final.zkey
ZK_SNARKJS_TIMEOUT_MS=120000

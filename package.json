{
  "name": "medoxie-exam-zk-prover",
  "version": "1.0.0",
  "private": true,
  "description": "HTTP service for Medoxie exam-pass Groth16 proofs (Railway / Docker)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "snarkjs": "^0.7.5",
    "tsx": "^4.19.2",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
